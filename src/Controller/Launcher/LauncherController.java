package Controller.Launcher;

import Model.Model;
import View.View;

public class LauncherController extends Controller.Controller {
    
    public LauncherController(Model model, View view) {
        super(model, view);
    }
    
}
