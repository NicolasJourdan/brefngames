package Training.Controller;

import Scene.Controller.AbstractSceneController;
import Scene.Model.Scene;
import Structure.AbstractModel;
import Structure.AbstractView;

public class TrainingMenuController extends AbstractSceneController {

    public TrainingMenuController(AbstractModel model, AbstractView view, Scene scene) {
        super(model, view, scene);
    }
}
